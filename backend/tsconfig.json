{
  "extends": "../tsconfig.json",
  "compilerOptions": {
    "outDir": "../dist/backend",
    "typeRoots": ["node_modules/@types", "../types"]
  },
  "include": ["**/*.ts", "**/*.env"],
  "exclude": ["node_modules"]
}
